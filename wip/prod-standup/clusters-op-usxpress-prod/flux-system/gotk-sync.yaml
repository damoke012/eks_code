# This manifest was generated by flux. DO NOT EDIT.
# Prod version: identical to op-qa except the reconcile path points at THIS
# cluster's directory. `flux bootstrap` with flux_target_path=clusters/op-usxpress-prod
# regenerates exactly this, so pre-writing it is idempotent.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: master
  secretRef:
    name: flux-system
  url: https://github.com/variant-inc/iaac-talos-flux-cluster.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/op-usxpress-prod
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
