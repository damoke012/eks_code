apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# PER-BRANCH. The platform repo carries one branch per cluster (op-dev / op-qa /
# op-prod). List only the ApplicationSet for this cluster.
resources:
  - applicationset-qa.yaml
