# PR review — iaac-talos-flux-cluster #7 (Idris, INFRA-1487)

Reviewer: Doke
Date: 2026-05-29
Decision: **REQUEST CHANGES** (3 blockers, 1 data-loss-risk verify, several smaller items)

> **Round 1 comment posted**: https://github.com/variant-inc/iaac-talos-flux-cluster/pull/7#issuecomment-4578051242 (2026-05-29 ~13:27 CT)
>
> **Idris response (Teams, 2026-05-29 13:36 CT)**:
> - "ill update PR with comments thanks" — fixes incoming
> - "the stateStore bucket is `risingwave-state-op-usxpress-dev`" — **clears Blocker #2** (data-loss risk). Patch matches live bucket. ✅
>
> **Remaining**: Blocker #1 (postgres password), Blocker #3 (operator version pin), verify #4 (placeholders + secret wiring + trailing newline), coordination asks (branch test + Tim).

---

## PR identity

| | |
|---|---|
| Repo | `variant-inc/iaac-talos-flux-cluster` |
| PR | [#7](https://github.com/variant-inc/iaac-talos-flux-cluster/pull/7) |
| Title | *feat(risingwave): add GitRepository and Kustomization for iaac-risingwave-onprem* |
| Author | Idris Fagbemi (`ifagbemi-usxpress`) |
| Branch | `INFRA-1487-add-iaac-risingwave-onprem-to-flux` → `master` |
| Latest commit | `4bbd5ab` (2026-05-29 17:15 UTC) |
| State | OPEN, MERGEABLE, reviewDecision=CHANGES_REQUESTED |
| Jira | [INFRA-1487](https://usxpress.atlassian.net/browse/INFRA-1487) |

Two commits on the branch:
- `c359d51` (2026-05-28) — initial add of GitRepository + Kustomization
- `4bbd5ab` (2026-05-29) — adds resource patches + healthCheckExprs (the commit that triggered this review)

## What the PR does

Brings Tim's `risingwave` ns workload under Flux management. Before this PR, Tim's RW was Idris's hand-applied kubectl deployment with drift risk (per [[rw-manifest-landscape-2026-05-28]]). After this PR, `variant-inc/iaac-risingwave-onprem` `main` is Flux's source of truth for the workload at `./manifests/op-usxpress-dev`.

The PR appends ~116 lines to [`clusters/bm-dev/flux-system/infra.yaml`](https://github.com/variant-inc/iaac-talos-flux-cluster/blob/INFRA-1487-add-iaac-risingwave-onprem-to-flux/clusters/bm-dev/flux-system/infra.yaml):

1. `GitRepository iaac-risingwave-onprem` (interval 5m, branch `main`)
2. `Kustomization risingwave-onprem` with:
   - `path: ./manifests/op-usxpress-dev`
   - `prune: false` (GOOD — won't delete hand-applied resources Tim has)
   - `dependsOn: external-secrets`
   - Four resource patches (RisingWave CR + postgres HR + prometheus HR + risingwave-operator HR)
   - `healthCheckExprs` (CEL on RW Running condition)
   - `wait: true`, `timeout: 10m`

## Findings

### BLOCKER 1 — Hardcoded postgres password in Git

```yaml
- op: add
  path: /spec/values/global
  value:
    postgresql:
      auth:
        password: risingwave   # ← literal secret in Git
```

`existingSecret: risingwave-pg-credentials` is already set, but the literal `password: risingwave` is committed to the Flux-watched repo. Two problems:
- Audit / CySec / Wiz will flag this as a secret in source control.
- Acts as a silent fallback if `existingSecret` ever fails to resolve — worst kind of compromise mode.

**Fix**: remove the entire `/spec/values/global` add operation. `existingSecret` alone is sufficient.

### BLOCKER 2 (DATA LOSS RISK) — stateStore S3 bucket replacement

```yaml
- op: replace
  path: /spec/stateStore
  value:
    dataDirectory: hummock
    s3:
      bucket: risingwave-state-op-usxpress-dev
      credentials:
        accessKeyRef: AccessKeyID
        secretAccessKeyRef: SecretAccessKey
        useServiceAccount: true
      region: us-east-2
```

Replaces the entire stateStore block. If the running RW currently writes hummock to a different bucket name (e.g., legacy on-prem bucket), this PR points compute pods at `risingwave-state-op-usxpress-dev` — which may not exist OR may be empty.

**Effect**: compute pods come up trying to recover from no hummock → effective data loss for Tim.

**Must verify before approve**:
```bash
# Current bucket on the live RW
kubectl get rw risingwave -n risingwave \
  -o jsonpath='{.spec.stateStore.s3.bucket}{"\n"}'

# Confirm bucket exists + has hummock data in 700736442855 (USX-Dev)
aws s3 ls s3://risingwave-state-op-usxpress-dev/hummock/ --profile usx-dev | head
```

If the live bucket name differs from the patch value, **DO NOT merge** until either:
- Patch updates to match live bucket name, OR
- A migration plan exists (copy hummock data, then switch).

### BLOCKER 3 — risingwave-operator version float

```yaml
- op: replace
  path: /spec/chart/spec/version
  value: ">=0.1.0 <0.2.0"
```

Range selector — Flux/Helm picks the latest 0.1.x on any reconcile. Operator upgrades can trigger CR reconciliation → compute pod restart on Tim.

**Fix**: pin to the exact version currently running:
```bash
kubectl -n risingwave get hr risingwave-operator -o jsonpath='{.spec.chart.spec.version}{"\n"}'
```
Use that exact value (e.g., `0.1.5`).

### VERIFY — Earlier review items from 2026-05-28

The May 28 request-changes asked Idris to:
1. Strip 3 placeholder comments from `infra.yaml`
2. Fix double `---` + missing trailing newline
3. Confirm `rw-secret-private-key.yaml` wires env var onto RW CR (not just creates Secret)

The diff hunk shown (`@@ -513,3 +513,116 @@`) only displays the appended block. **Cannot confirm from this diff** whether items 1 + 3 are addressed. Trailing newline is still missing (`\ No newline at end of file`).

Need Idris to confirm in a PR reply.

## Smaller / non-blocker observations

- **`dependsOn: external-secrets`** — assumes there's a Kustomization literally named `external-secrets` in the same scope. Verify the actual name (might be `infra-external-secrets` or similar).
- **Compute nodeGroup resource changes**: `cpu 250m / mem 512Mi requests`, `cpu 1 / mem 1Gi limits`. If current values differ → compute pod restart on first reconcile. Tim should know.
- **`prune: false`** — RIGHT call. Critical for not auto-deleting Tim's hand-applied resources.
- **healthCheckExprs CEL** — clean, correct semantics (`Running=True` is current, `Running=False` is failed, `Upgrading=True` is inProgress).
- **Missing trailing newline** — minor lint.

## Coordination asks (regardless of code fixes)

Per [[feedback_protect_rw_onprem_workload]]:

1. **Pre-merge branch test**: Idris must reconcile this branch against the live cluster (point an existing Kustomization at the branch, or `flux build` + `flux diff`) and watch Tim's RW stay `Running=True` end-to-end. Confirm in PR reply.
2. **Tim alignment**: even after code fixes, compute resource changes → pod restart → brief unavailability. Tim must acknowledge the reconcile window.
3. **Rollback plan**: Tim's hand-applied YAML should be saved somewhere (Idris's local? branch?) so reverting to hand-applied is a single `kubectl apply` if Flux misbehaves.

## Draft PR comment

Posted version goes here once Idris responds.

```
Thanks for the iteration. Three blockers + one verify-please before I can approve:

1. (BLOCKER) Hardcoded postgres password.
   /spec/values/global.postgresql.auth.password: risingwave is a literal secret
   committed to the Flux-watched repo. Please remove the entire /spec/values/global
   patch — existingSecret: risingwave-pg-credentials alone is sufficient; the chart
   resolves the password from the Secret. The literal also acts as a silent fallback
   if existingSecret ever fails to resolve, which is the worst kind of compromise mode.

2. (DATA LOSS RISK) stateStore bucket replacement.
   The patch replaces /spec/stateStore to point at risingwave-state-op-usxpress-dev.
   Please confirm this matches what the running RW uses today:
       kubectl get rw risingwave -n risingwave -o jsonpath='{.spec.stateStore.s3.bucket}'
       aws s3 ls s3://risingwave-state-op-usxpress-dev/hummock/ --profile usx-dev
   If the live bucket name differs, this PR will point compute pods at an empty
   bucket and effectively wipe Tim's state on first reconcile.

3. (BLOCKER) Operator version float.
   ">=0.1.0 <0.2.0" lets Flux auto-bump the operator on any reconcile and can
   cascade into pod restarts on Tim's RW. Pin to the exact version currently running.

4. (verify) Earlier review items.
   Are the placeholder comments above the new GitRepository block stripped, and is
   rw-secret-private-key.yaml wiring the env var onto the RW CR? Diff only shows the
   appended hunk so I can't tell. Please confirm.

Coordination ask: before you merge, please confirm
- You've reconciled this branch against the live cluster (point an existing
  Kustomization at your branch temporarily, or flux build/flux diff against the
  cluster) and watched Tim's RW stay Running=True end-to-end.
- Tim has acknowledged the reconcile window — even with the fixes above, compute
  pod resources change → pod restart → brief unavailability.
```

## Apply

- Read this doc before next interaction on PR #7.
- When Idris pushes updates, append a section "Round 3 review (<date>)" to this file.
- On merge: capture lessons in [[rw-manifest-landscape-2026-05-28]] (which becomes outdated by this merge — that file currently says Tim's RW is NOT Flux-managed).

---

# Round 2 review — independent verification (2026-05-29 PM)

After Idris's response on Round 1, we ran our own verification against the live cluster + cloned `iaac-risingwave-onprem` main + cloned PR #7 branch. **Posted as comment-4578141644.**

## What we verified ourselves

### Live cluster checks (kubectl)

```
# stateStore on live RW
bucket: risingwave-state-op-usxpress-dev    ✅ matches patch
region: us-east-2                            ✅ matches patch
dataDirectory: hummock                       ✅ matches patch
useServiceAccount: true                      ✅ matches patch

# RW baseline
Running=True Upgrading=False                 ✅ healthy baseline

# Compute resources LIVE
risingwave-compute-default-0:
  req={"cpu":"250m","memory":"512Mi"}        ✅ matches patch
  lim={"cpu":"1","memory":"1Gi"}             ✅ matches patch
```

### S3 bucket contents

```
aws s3 ls s3://risingwave-state-op-usxpress-dev/hummock/
PRE 0/ ... PRE 106/   (107+ hummock prefixes — live state)
```

Bucket exists, has data. **Truly no data-loss risk on stateStore patch.**

### Pods snapshot (catches the 2-postgres issue)

```
pg-postgresql-0          29d   ← RW meta connects HERE
postgres-postgresql-0    4h17m ← provisioned but NOT used
risingwave-operator      3h52m (restarted)
risingwave-meta-default  3h28m (restarted)
prometheus stack         4h31m (came up)
postgres-postgresql      4h17m (came up)
```

Recent 4h activity = Idris pre-deployed iaac-risingwave-onprem source manifests (likely branch-test).

### Meta postgres wiring (the catalog-loss check)

```
RW CR metaStore.host:    pg-postgresql.risingwave.svc.cluster.local
Meta pod RW_SQL_ENDPOINT: pg-postgresql.risingwave.svc.cluster.local:5432
```

Both point at OLD postgres (pg-postgresql) — the 29d hand-deployed one.

### Source repo cross-check (iaac-risingwave-onprem main)

```
risingwave-cr.yaml:28:  host: pg-postgresql.risingwave.svc.cluster.local
```

Source CR ALSO points at pg-postgresql — matches live. **No metaStore rewire on merge → no catalog loss.**

### Source repo postgres HR mismatch

`postgres-helmrelease.yaml` in source provisions a new `postgres` HR (currently `postgres-postgresql-0`, 4h old) but:
- CR doesn't reference it
- Console doesn't reference it
- Nothing in source references it

= dead code in source. Cluster shows it running idle.

### Legacy `pg-postgresql` not in source

The postgres RW actually uses is **hand-deployed and not in source manifests** (verified — no `pg-postgresql-helmrelease.yaml` exists). After this PR lands, `prune: false` preserves it but it stays outside GitOps.

### Secret-key wiring (the May 28 carry-over)

```
# ExternalSecret creates k8s Secret rw-secret-store-private-key
# with key RW_SECRET_STORE_PRIVATE_KEY_HEX

# But the RW CR env block contains ONLY:
env:
  - name: RW_PROMETHEUS_ENDPOINT
    value: http://prometheus-server.risingwave.svc.cluster.local
```

No `envFrom`, no `secretKeyRef`, no reference to `RW_SECRET_STORE_PRIVATE_KEY_HEX`. **The Secret is created and orphaned.** Was open from May 28 round, still open in `4bbd5ab`. **Promoted to BLOCKER #6** because it affects Tim's RW built-in secret manager (per [[tim_meeting_2026_05_26_sql_pipeline.md]]).

## Final verdict matrix

| # | Item | State | Severity |
|---|---|---|---|
| 1 | Hardcoded postgres password | OPEN | BLOCKER (Idris fixing) |
| 2 | stateStore bucket | ✅ CLEARED | — |
| 3 | operator version float | OPEN | BLOCKER (Idris fixing) |
| 4a | Placeholder comments | ✅ CLEARED | — |
| 4b | EOF newline + trailing whitespace | OPEN | Lint |
| 5 | metaStore catalog loss | ✅ CLEARED | — |
| 6 | **RW secret key NOT wired to CR env** | OPEN | **BLOCKER (promoted)** |
| 7 | Compute resource restart concern | ✅ NO RESTART | — |
| A1 | Dead `postgres` HR in source | OPEN | Advisory |
| A2 | `pg-postgresql` not in source | OPEN | Advisory |

## Comment posted

- **URL**: https://github.com/variant-inc/iaac-talos-flux-cluster/pull/7#issuecomment-4578141644
- **Timestamp**: 2026-05-29 PM
- **Content**: Cleared items + 3 still-open blockers + 2 advisories + coordination ask

## Awaiting from Idris

1. Fix Blocker 1 (remove hardcoded password)
2. Fix Blocker 3 (pin operator version)
3. Fix Blocker 6 (wire RW secret key env var onto the CR in `iaac-risingwave-onprem` repo)
4. Address advisories (or commit to follow-up PRs)
5. Confirm branch-test + Tim coord

---

# Round 3 review — `d2dfdd5` (2026-05-29 PM)

Idris pushed commit `d2dfdd5` addressing all 3 remaining blockers. **Posted as [comment-4578356999](https://github.com/variant-inc/iaac-talos-flux-cluster/pull/7#issuecomment-4578356999).**

## Changes in `d2dfdd5`

### Blocker 1 — hardcoded password ✅ REMOVED
Entire `/spec/values/global` add op deleted from postgres HR patch block.

### Blocker 3 — operator version pinned ✅
`">=0.1.0 <0.2.0"` → `"0.1.36"`. Matches live operator HelmChart artifact `0.1.36` (from earlier event check). Clean pin.

### Blocker 6 — RW secret key wiring ✅
Two patch ops added:
```yaml
- op: add
  path: /spec/components/meta/nodeGroups/0/template/spec/env/-
  value:
    name: RW_SECRET_STORE_PRIVATE_KEY_HEX
    valueFrom:
      secretKeyRef:
        name: rw-secret-store-private-key
        key: RW_SECRET_STORE_PRIVATE_KEY_HEX

- op: add
  path: /spec/components/frontend/nodeGroups/0/template/spec/env
  value:
    - name: RW_SECRET_STORE_PRIVATE_KEY_HEX
      valueFrom:
        secretKeyRef:
          name: rw-secret-store-private-key
          key: RW_SECRET_STORE_PRIVATE_KEY_HEX
```

### Lint cleanup ✅
Trailing whitespace + EOF newline appear addressed in diff.

### .gitignore addition
`POSTGRES_FOLLOWUP_PLAN.md` added to .gitignore — Idris has a local plan for the postgres cleanup. Flagged for visibility.

## JSON patch path verification

Critical: `add` to `env/-` requires env array to exist; `add` to `env` requires it NOT to exist. Verified against source CR:

```
meta: env=[{'name': 'RW_PROMETHEUS_ENDPOINT', 'value': '...'}]      ← env/- appends ✅
frontend: env=None                                                   ← env creates ✅
compute: env=None
compactor: env=None
```

Both patch ops will apply cleanly at Flux reconcile.

## Scope clarification — Tim coord rule (locked in 2026-05-29)

User explicit: Tim coord required ONLY when ns = `risingwave` (Tim's). Not required for `risingwave-2`.

Verified PR #7 target ns: `risingwave` (every resource in source has `namespace: risingwave` or `risingwave-operator-system`; zero `risingwave-2` references). **Tim coord IS required for this PR.**

Memory updated: [[feedback_protect_rw_onprem_workload]] now has the namespace-scope clarification.

## Final review status

| # | Item | State |
|---|---|---|
| 1 | Hardcoded postgres password | ✅ FIXED |
| 2 | stateStore bucket | ✅ CLEARED (Round 2 verify) |
| 3 | Operator version float | ✅ FIXED |
| 4a | Placeholder comments | ✅ CLEARED |
| 4b | EOF newline + lint | ✅ FIXED |
| 5 | metaStore catalog loss | ✅ CLEARED (Round 2 verify) |
| 6 | RW secret key wiring | ✅ FIXED + JSON path verified |
| 7 | Compute resource restart | ✅ NO RESTART (Round 2 verify) |
| A1 | Dead `postgres` HR in source | OPEN — follow-up; Idris has local plan |
| A2 | `pg-postgresql` not in source | OPEN — follow-up |

**All code blockers cleared. Only coordination gates remain.**

## What's left for approve

1. Idris confirms branch test ran clean against live (Tim's RW stayed Running=True end-to-end)
2. Tim acknowledges reconcile window (meta + frontend env additions → pod restart)

Once both → approve + merge.
