# Idris correspondence — 2026-05-27 → 2026-05-28

Captured for record. Key handoff: Idris owns RW (Phase 1 Tim's `risingwave` ns + RW-2) going forward; Doke's focus moves back to networking.

---

## 2026-05-26 (context — Tuesday)

**Idris:** "Tim already has a repo called risingwave-pipeline, so i am creating a new one with risingwave-platform-pipeline"

(Doke later realized Idris was looking at Doke's fork `variant-inc/risingwave-pipeline`, which Doke had created the previous evening from `usxpressinc/risingwave-poc`.)

---

## 2026-05-27 — morning

**Doke:** "Hi Idris Fagbemi GM. Whats the plan for today"

**Idris:** "will be couple of hours out and when i'm back i will create the qa env"

**Doke:** "Ok cool. When you are back let us connect."

**Doke** (later, before Idris was back): *"wait before you create risingwave-platform-pipeline. I already forked Tim's usxpressinc/risingwave-poc into variant-inc/risingwave-pipeline yesterday evening (after the meeting). My feat/onprem-rw2-adaptation branch (commit 72b62f2) adapts the SM paths to /risingwave-2/* and the IAM role names for our cluster. Also did a Flyway-on-RW spike last night. NOTE: Flyway is incompatible with RW (rejects VARCHAR(N) length specifiers + no read-write transactions). So your feat/rw_pipeline file-change-detection approach is actually the right call for RW. I want to extend it (not replace) with a lightweight RW-compatible tracking table + apply-all bootstrap mode. Details in the progress log. Can we sync when you're back (you mentioned a couple of hours)? We're on the same page, just need to not create a duplicate repo. Use variant-inc/risingwave-pipeline as the source of truth. Also still a lot of things to do in Dev. P.S. — what did you see at risingwave-pipeline that you thought was Tim's? I might be missing something."*

**Idris:** "Alright"

---

## 2026-05-27 — afternoon (handoff)

**Idris:** "i'm back"

**Doke:** "Ok ill ping you still working on a few things. Spent the whole day building the SQL CICD Pipeline into RW-2. Ill share the Readme in a few"

**Idris:** "ok"

**Doke:** *(shares link)* `risingwave-pipeline/ONPREM_CICD.md at feat/onprem-rw2-adaptation · variant-inc/risingwave-pipeline`

**Idris:** "should i add my secret integration"

**Doke (key handoff):** "Its yours from here. ok. Please take it to the next level. then from here push to Tim's NS when you feel it should go there. Ill give you a write up shorly and ill need to logoff and we can pick up tomorrow"

**Idris:** "Ok, sounds good"

**Doke:** "My focus will move back to networking so RW will be in your hands"

**Idris:** "Sounds good"

---

## 2026-05-27 — Doke's written handoff

Pasted in full to Idris (matches the planned Idris-task rundown). Key task list — **#4 was added by Doke beyond the original draft**:

> 1. Review the branch + ONPREM_CICD.md and sign off on the approach (file-change detection, not Flyway — Flyway's a dead end on RW: VARCHAR(N) + no read-write transactions).
> 2. Pick + set the required reviewer(s) on the pipeline-approval GitHub Environment (you / Tim / me).
> 3. I'll add the tracking table + apply-all bootstrap to pipeline.yaml (the Flyway replacement) — I'll ping you to review the schema before we PR.
> **4. add secrets and user creation into the Platform which the apps can handle in the pipeline and do any other engineering work needed to TIM**

On Doke (Octopus/infra): TfApply safety flip + the small repo cleanups (kill the failing build.yaml "Push to Octopus" + remove the healthcheck test files). Then merge to master.

On QA: hold off for now.

---

## 2026-05-28 — RW_SECRET_STORE_PRIVATE_KEY_HEX

**Idris:** "Hi Dare Oke. I need to add RW_SECRET_STORE_PRIVATE_KEY_HEX to the risingwave console. I have updated it on the script to deploy locally but i haven't applied yet. The secret has been created for both risingwave and risingwave-2"

**Doke:** "Cool — before you apply, two things: (a) ping Tim to align on the namespace restart timing (Tim is actively using the ns — coord with him) (brief pod roll on risingwave ns), and (b) make sure the env var lands in the RW manifest in iaac-talos-flux-platform/infrastructure/risingwave/ (op-dev) via PR, not just a local-deploy script — otherwise Flux reconciles it away. Same for risingwave-2."

**Idris:** *(link)* "risingwave is not in iaac-talos-flux-platform/infrastructure at op-dev · variant-inc/iaac-talos-flux-platform"

**Doke:** "Sorry — my mistake, that path was stale on my end. Can you point me at the right repo / path for both manifests? Same gitops point still stands: wherever it actually lives, the env var should land via PR, not just the local-deploy script. Once you tell me the path I can sanity-check the diff if helpful."

**Awaiting Idris reply** with the actual manifest path. Likely `variant-inc/iaac-risingwave-2` (separate repo) and possibly the same for `risingwave` ns.

---

## What this thread changes

- **Ownership:** Idris now owns RW end-to-end (Phase 1 `risingwave` + Phase 2 `risingwave-2` SQL CICD). Doke supports on the Octopus/infra side only.
- **Doke's focus:** moves back to networking (HTTPS + TCP + subzone delegation, Steve message still draft).
- **New work item for Idris (#4 in the rundown):** secrets + user creation into the Platform so apps can handle them in the pipeline + any other engineering work needed to/for Tim. This is broader than the current SQL pipeline scope.
- **Stale memory corrected:** the path `iaac-talos-flux-platform/infrastructure/risingwave/` does NOT exist on op-dev (per Idris's direct link). Memory `iaac_risingwave_archived.md` updated 2026-05-28 to flag this and tell future sessions to verify rather than assume.
