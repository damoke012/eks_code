# PR #73 review — Round 1 (2026-07-10)

**variant-inc/iaac-talos-flux-platform #73** — "Chore/flux argocd repo sync" by ifagbemi-usxpress → op-dev.

Codespace cannot reach variant-inc (private org) or the cluster. Round 1 is from the PR description + commit list; live verification pending on WSL.

## Commits
- 9953ad1 fix(grafana): resolve No Data — pin Prometheus nameOverride + add sto…
- 8a43e94 Merge branch 'op-dev' …
- 76c50b5 Add Grafana dashboards for RisingWave streaming + update kustomization
- f0b0265 refactor(prometheus): remove outdated Grafana "No Data" troubleshooting…
- c9ca35c Manage Argo CD via Flux with NodePort and repo-sync manifests

## Findings (see STATE.md for the table). Verdict: HOLD.

## WSL verification battery

### Phase 1 — pull the real diff
```bash
gh pr view 73 --repo variant-inc/iaac-talos-flux-platform \
  --json title,state,headRefName,baseRefName,headRefOid,mergeable,additions,deletions,changedFiles,files
gh pr diff 73 --repo variant-inc/iaac-talos-flux-platform > /tmp/pr73.diff; wc -l /tmp/pr73.diff
# account for the 67k — biggest files first
gh pr view 73 --repo variant-inc/iaac-talos-flux-platform \
  --json files -q '.files[] | "\(.additions)\t\(.path)"' | sort -rn | head -20
```

### Phase 2 — namespace (coord gate) + secret hygiene
```bash
gh repo clone variant-inc/iaac-talos-flux-platform /tmp/itfp -- --depth 1 --branch chore/flux-argocd-repo-sync
cd /tmp/itfp
# #3 what ns / repo does the RW Argo CD app target?
cat infrastructure/argocd/risingwave-repo-sync/repo_sync.yaml
grep -rE 'namespace:|destination:|server:|repoURL:|project:' infrastructure/argocd/risingwave-repo-sync/
# #2 does the git secret contain real key material?
grep -iE 'sshPrivateKey|privateKey|BEGIN [A-Z]+ PRIVATE KEY|^\s*data:|stringData:' infrastructure/argocd/argocd_git_secret.yaml
```

### Phase 3 — dual-controller + RW baseline (live cluster)
```bash
# #1 is RisingWave already Flux-managed? who owns it?
flux get kustomizations -A | grep -i rising
kubectl get kustomization -A | grep -i rising
# RW health baseline (must be Running before/after)
kubectl get rw -A
kubectl get pods -n risingwave -o wide
kubectl get pods -n risingwave-2 -o wide
# is Argo CD already installed / pre-applied?
kubectl get ns argocd 2>/dev/null && kubectl get applications,pods -n argocd 2>/dev/null
# would the Argo CD app's destination ns overlap resources Flux reconciles?
```

## Round-1 comment (draft — post with `gh pr comment 73 --repo variant-inc/iaac-talos-flux-platform --body-file`)

> Round 1 — reviewed head `c9ca35c` from the description; holding for verification.
>
> Thanks Idris. This is bigger than a chore — it puts Argo CD alongside Flux as a second GitOps controller managing RisingWave, so I need a few things confirmed before I can approve:
>
> **Blockers**
> 1. (BLOCKER) Flux vs Argo CD ownership — which controller exclusively owns the RisingWave resources this app syncs? We can't have both reconciling the same objects. Please spell out the boundary (namespace/resources Argo CD owns, and confirm Flux no longer reconciles them).
> 2. (BLOCKER) `infrastructure/argocd/argocd_git_secret.yaml` — does this contain real SSH key material? If so it needs to come from Secrets Manager via an ExternalSecret, not be committed. Per our CA/secrets model SM is the source of truth.
> 3. (BLOCKER) Which namespace does `risingwave-repo-sync` target — `risingwave` or `risingwave-2`? If it's `risingwave`, I need Tim's sign-off before merge.
> 4. (BLOCKER) +67,612 lines — can you confirm what makes up the bulk (streaming dashboards?) so we know nothing unexpected (vendored CRDs, generated files, secrets) is riding along?
>
> **Advisory (not blocking, follow-ups fine)**
> 5. Argo CD server on NodePort 31311/32119 — we now have the Istio ingress + LE cert plane; can this go through it instead of a raw NodePort?
> 6. `ServerSideApply=true` — fine in isolation, but confirm it isn't applying over Flux-owned fields (see #1).
> 7. Could the Grafana "No Data" fix + RW streaming dashboards be split into their own PR? Keeps the Argo CD change independently revertible.
>
> **Before merge, please confirm:** you tested this on op-dev with `kubectl get rw -A` Running before AND after, and (if #3 = `risingwave`) Tim has signed off.
