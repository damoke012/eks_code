apiVersion: v1
kind: ServiceAccount
metadata:
  name: velero
  namespace: velero
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::700736442855:role/op-usxpress-dev-velero
    # Trust policy: must allow:
    #   federated: arn:aws:iam::700736442855:oidc-provider/d3a7wcnazdrd6p.cloudfront.net
    #   condition: StringEquals { d3a7wcnazdrd6p.cloudfront.net:sub: "system:serviceaccount:velero:velero" }
    # Policy: AmazonS3FullAccess scoped to backup bucket
    #   arn:aws:s3:::velero-op-usxpress-dev/*
    # Terraform module: see iaac-talos PR for op-usxpress-dev-velero role.
