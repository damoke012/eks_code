---
name: user-doke-onprem-platform
description: "Who the user is — Doke/Dare Oke, on-prem platform + cloud engineer at US Xpress"
metadata: 
  node_type: memory
  type: user
  originSessionId: 161fed6b-7af8-49e8-9abf-c06ed6494c28
---

User is **Doke** (full name Dare Oke; git author "Doke <damoke012@gmail.com>"). Cloud/platform engineer at **US Xpress** (Atlassian org `usxpress.atlassian.net`, Confluence space `UI`).

Owns the **on-prem Talos Kubernetes platform** (clusters `op-usxpress-dev`, `op-usxpress-qa`) built with Flux + Terraform + Octopus Deploy. GitHub org for IaC repos is **`variant-inc`** (e.g. `variant-inc/iaac-talos-flux-platform`, `iaac-talos`). Also works with EKS. Doke reviews and applies teammates' PRs (Idris authors, Doke reviews/applies). Does NOT have Azure/Entra access (Parul does).

**People:** Idris (ifagbemi; implementer, owns `risingwave` ns track), Tim (RW-2 SQL consumer), Rohit Saini (teammate; checks cluster namespaces/deployments), Mark (drives Entra rotation, validates lower envs), Parul (Entra-rotation operator — Azure access being granted, does NOT have it yet), Steve (grants Azure access via Marvel, knows app owners; the Entra-thread CC), Marvel (Azure admin), Buddy (app owner — inherited Garrett's apps incl. XPM/cadex; Garrett left), Srikanth / Jason (mulesoft contacts), Steve Duck (Networking), Brendan Buschel (Cyber Security), Steve Vives (Wiz / security tooling), John Quick Griffin (registrar / DNS). Data-lake team: Nathaniel + Anthony (Kafka→S3 migration), Matt (platform, Grafana/alerting), Vivint/Vivian (Snowflake DX repo). Josh Gilliland ("Josh G") — FreshService admin (ticketing/notification, replaced PagerDuty). **Mark = a NEW team member being onboarded** (K8s-upgrade ramp, Wiz, failure reviews) — distinct from the architect voice transcripts mislabel as "Mark"/"Dean" (that voice = Dare/you). Diarization across forwarded transcripts is unreliable — verify names before assigning.

Works from Jira (INFRA-* and MAN-* tickets), publishes designs to Confluence, and journals via dated commit messages ("jun24:", "qa-standup:") + `wip/` and `scratchpad/` workspaces. Prefers moving fast and turning anything deferrable into a follow-up ticket.
